spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/payflow_settlement
  kafka:
    bootstrap-servers: kafka:9092

eureka:
  client:
    service-url:
      defaultZone: http://service-registry:8761/eureka/
